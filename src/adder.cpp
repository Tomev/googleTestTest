#include "adder.h"

double adder::add(double x, double y)
{
  /** Add method with doubles as attributes. Returns sum of two doubles.
   *
   *  @brief Returns sum of two double addends.
   *
   *  @return x + y
   */

  return x + y;
}

int adder::add(int x, int y)
{

}